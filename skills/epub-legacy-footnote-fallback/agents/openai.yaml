interface:
  display_name: "EPUB 旧版弹注 Fallback"
  short_description: "在标准弹注上叠加多看旧版兼容 fallback。"
  default_prompt: "使用 $epub-legacy-footnote-fallback 在这个标准 EPUB 弹注结构上叠加多看旧版兼容 fallback，并保持 SPEC §1 结构不变。"
