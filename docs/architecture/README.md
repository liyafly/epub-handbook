# 周边 / 下游架构参考

此目录收录 `epub-handbook` 之外、但需要在仓内保留副本的架构说明。

## 当前条目

- [epub-pro-v1.md](epub-pro-v1.md)：下游 `epub-pro` 实现仓的技术架构正本副本。

## 维护约定

- handbook 是上游规范源；`epub-pro` 是下游实现。
- 下游架构更新时，同步更新本目录的副本，并在文件顶部注明同步日期与上游 commit。
- 本目录文件不进 `docs/final/`：它们不是 `epub-handbook` 的对外硬约束。
