# 下游参考文档索引

此目录用于收纳**下游实现仓**相关的参考性文档说明，避免与 `docs/final/` 的上游实现约束混淆。

## 当前条目

- `epub-pro` 技术架构正本：`docs/final/epub-pro 技术架构 v1.md`（当前仓保留副本，供对接阅读）。

## 维护约定

- handbook 是上游规范源；若下游 `epub-pro` 架构更新，应在本仓同步更新该副本并注明更新时间。
