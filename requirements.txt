# Target runtime: Python 3.14 (pinned via .python-version; uv/mise).
# Recommended for guide snippets and future OPF/XHTML utilities.
# Core red-line scripts currently use Python stdlib xml.etree and do not require it.
lxml>=4.9
