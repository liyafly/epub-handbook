# uv environment metadata. This repository is script-first, not a package.
[project]
name = "epub-handbook"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
  "lxml>=4.9",
]

[tool.uv]
package = false
