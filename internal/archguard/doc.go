// Package archguard 把 docs/final/SPEC-go-architecture.md 的架构约束
// 编码成可执行测试。它是架构的**唯一权威定义**：文档描述架构，本包*断言*架构。
//
// # 规则 0
//
// 禁止修改本目录下的任何文件。
//
// 守卫红了，意味着改动违反了架构，不是守卫写错了。
// 正确反应是改你的代码；错误反应是改守卫、加白名单、注释掉断言、或打 t.Skip。
// 若确信守卫本身有误,停下来报告给人类,不要自行放宽。
//
// # 分层
//
// Tier A（静态）：纯 AST / 文件扫描，不 import 任何项目包。
// 从仓库第一天就能跑，且不会被别处的编译错误带崩。
// 覆盖 §1 依赖方向、INV-2、INV-3、INV-4、INV-5、INV-7。
//
// Tier B（行为）：需要真实代码才能验证，覆盖 INV-1、INV-6。
// 在对应包尚未创建时以「bootstrap 跳过」方式让路，一旦目录存在便无法再跳过。
package archguard
